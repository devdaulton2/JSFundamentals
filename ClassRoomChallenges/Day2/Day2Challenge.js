let = myName = "Aryn"
let = friendName = "Joe"
console.log(myName.length, friendName.length)

if (myName.length > friendName.length) {
    console.log(myName, "is longer than", friendName)
} else {
    console.log(`${friendName} is longer than ${myName}.`) 
} 

if (myName.length > friendName.length) {
    console.log(`My name is longer than ${friendName}`)
} else if (myName.length < friendName.length) {
    console.log(`${friendName} is longer than my name by ${friendName.length-myName.length}`)
} else {
    console.log(`Our names are the same length.`)
}

let myName = "Aryn";
let friend = "Tony"

let nameDebate = (myName.length > friend.length) > (`${friendName}`))
console.log(nameDebate);